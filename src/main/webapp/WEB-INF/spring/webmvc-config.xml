<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:context="http://www.springframework.org/schema/context" xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:p="http://www.springframework.org/schema/p" xmlns:sec="http://www.springframework.org/schema/security" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd     http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd     http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">
	<sec:global-method-security pre-post-annotations="enabled">
		<sec:expression-handler ref="methodSecurityExpressionHandler"/>
	</sec:global-method-security>
				
	<!-- The controllers are autodetected POJOs labeled with the @Controller annotation. -->
	<context:component-scan base-package="com.collabnet.ccf.ccfmaster" use-default-filters="false">
		<context:include-filter expression="org.springframework.stereotype.Controller" type="annotation"/>
	</context:component-scan>

	<!-- Turns on support for mapping requests to Spring MVC @Controller methods
	     Also registers default Formatters and Validators for use across all @Controllers -->
   	<!--
		Spring 6.0 flipped the default of trailing-slash matching from true to false, so
		"/api/hospitalentrys/" stopped matching @RequestMapping("/api/hospitalentrys"). The
		REST API and its tests are full of trailing slashes, so the old behaviour is asked
		for explicitly. The attribute is deprecated (Spring intends URLs to be canonical) but
		flipping it here is the migration-faithful choice; changing every URL would be an API
		change.
	-->
	<mvc:annotation-driven conversion-service="applicationConversionService">
		<mvc:path-matching trailing-slash="true"/>
	</mvc:annotation-driven>
    <!--
	<!--
		Spring 6.0 flipped the default of trailing-slash matching from true to false, so
		"/api/hospitalentrys/" stopped matching @RequestMapping("/api/hospitalentrys"). The
		REST API and its tests are full of trailing slashes, so the old behaviour is asked
		for explicitly. The attribute is deprecated (Spring intends URLs to be canonical) but
		flipping it here is the migration-faithful choice; changing every URL would be an API
		change.
	-->
	<mvc:annotation-driven conversion-service="conversionService">
		<mvc:path-matching trailing-slash="true"/>
	</mvc:annotation-driven>
	<bean
	    id="conversionService"
		class="com.collabnet.ccf.ccfmaster.controller.CCFConversionServiceFactoryBean" />
	
	-->
	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources -->
	<mvc:resources location="/, classpath:/META-INF/web-resources/" mapping="/resources/**"/>
	
	<!-- Allows for mapping the DispatcherServlet to "/" by forwarding static resource requests to the container's default Servlet -->
	<mvc:default-servlet-handler/>
	
	<!-- register "global" interceptor beans to apply to all registered HandlerMappings -->
	<mvc:interceptors>
		<bean class="org.springframework.web.servlet.theme.ThemeChangeInterceptor"/>
		<bean class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor" p:paramName="lang"/>
	</mvc:interceptors>
	
	
	
	<!-- selects a static view for rendering without the need for an explicit controller -->
	<mvc:view-controller path="/login"/>
	<mvc:view-controller path="/teamForgeSessionTimeout"/>
    <mvc:view-controller path="/index" view-name="index"/> 
	<mvc:view-controller path="/uncaughtException"/>
	<mvc:view-controller path="/resourceNotFound"/>
	<mvc:view-controller path="/dataAccessFailure"/>
	<mvc:view-controller path="/accessDenied"/>
	<mvc:view-controller path="/update-core/success"/>
	<mvc:view-controller path="/update-core/failure"/>
	<mvc:view-controller path="/update-core/canceled"/>

	<!-- Resolves localized messages*.properties and application.properties files in the application to	allow for internationalization. 
		The messages*.properties files translate Roo generated messages which are part of the admin interface, the application.properties
		resource bundle localizes all application specific messages such as entity names and menu items. -->
	<bean class="org.springframework.context.support.ReloadableResourceBundleMessageSource" id="messageSource" p:basenames="WEB-INF/i18n/messages,WEB-INF/i18n/application" p:fallbackToSystemLocale="false"/>
	
	<!-- store preferred language configuration in a cookie -->
	<bean class="org.springframework.web.servlet.i18n.CookieLocaleResolver" id="localeResolver" p:cookieName="locale"/>	
	
	<!-- resolves localized <theme_name>.properties files in the classpath to allow for theme support -->
	<bean class="org.springframework.ui.context.support.ResourceBundleThemeSource" id="themeSource"/>
	
	<!-- store preferred theme configuration in a cookie -->
	<bean class="org.springframework.web.servlet.theme.CookieThemeResolver" id="themeResolver" p:cookieName="theme" p:defaultThemeName="ctf"/>

	<!-- This bean resolves specific types of exceptions to corresponding logical - view names for error views. 
	     The default behaviour of DispatcherServlet - is to propagate all exceptions to the servlet container: 
	     this will happen - here with all other types of exceptions. -->
	<!-- p:defaultErrorView="uncaughtException" -->
	<bean class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
		<property name="exceptionMappings">
			<props>
				<prop key=".PermissionDeniedDataAccessException">accessDenied</prop>
				<prop key=".DataRetrievalFailureException">resourceNotFound</prop>
				<prop key=".DataAccessException">dataAccessFailure</prop>
				<prop key=".NoSuchRequestHandlingMethodException">resourceNotFound</prop>
				<prop key=".TypeMismatchException">resourceNotFound</prop>
				<prop key=".MissingServletRequestParameterException">resourceNotFound</prop>
				<prop key=".AccessDeniedException">accessDenied</prop>
				<prop key=".BadRequestException">badRequest</prop>
				<prop key=".IllegalArgumentException">badRequest</prop>
				<prop key=".TFSessionExpiredException">teamForgeSessionTimeout</prop>
			</props>
		</property>
		<property name="statusCodes">
			<props>
				<prop key="resourceNotFound">404</prop>
				<prop key="accessDenied">403</prop>
				<prop key="badRequest">401</prop>
				<prop key="dataAccessFailure">500</prop>
				<prop key="teamForgeSessionTimeout">401</prop>
				<prop key="uncaughtException">500</prop>
			</props>
		</property>
		<property name="defaultErrorView" value="uncaughtException"/>
	</bean>
	
	<!-- allows for integration of file upload functionality -->
	<!-- Spring 6 removed the Commons-FileUpload multipart support;
	     StandardServletMultipartResolver is its replacement and relies on the
	     <multipart-config> element added to web.xml. -->
	<bean class="org.springframework.web.multipart.support.StandardServletMultipartResolver" id="multipartResolver"/>
<!--
    ==========================================================================================
    NOT MIGRATED. These two beans still name org.springframework.web.servlet.view.tiles2.*,
    which Spring stopped shipping in 4.0 (tiles2) and 6.0 (tiles3). There is nothing to move
    them to: Apache Tiles was retired to the Attic in 2018 and never produced a
    jakarta.servlet release, so neither Spring nor Tiles offers a jakarta-era Tiles view.

    This application's whole view layer is Tiles - /WEB-INF/layouts/layouts.xml plus the
    per-view views.xml files - so replacing it means choosing a different templating
    technology and re-authoring the layouts. That is a rewrite of the view tier, not a stack
    migration, and it would change behaviour, so it has deliberately been left alone rather
    than quietly disabled. The war compiles and packages; this context will not start until
    the view layer is dealt with separately. Nothing under src/test touches it - the test
    contexts have had the Tiles beans commented out since long before this migration.
    ==========================================================================================
  -->
<bean class="org.springframework.web.servlet.view.UrlBasedViewResolver" id="tilesViewResolver">
    <property name="viewClass" value="org.springframework.web.servlet.view.tiles2.TilesView"/>
  </bean>
    <bean class="org.springframework.web.servlet.view.tiles2.TilesConfigurer" id="tilesConfigurer">
    <property name="checkRefresh" value="true"/>
    <property name="definitions">
      <list>
        <value>/WEB-INF/layouts/layouts.xml</value>
        <!-- Scan views directory for Tiles configurations -->
        <value>/WEB-INF/views/**/views.xml</value>
      </list>
    </property>
  </bean>
	<!--Installs application converters and formatters-->
	<bean class="com.collabnet.ccf.ccfmaster.controller.ApplicationConversionServiceFactoryBean" id="applicationConversionService"/>
	
	
<import resource="webflow-config.xml"/>
</beans>
