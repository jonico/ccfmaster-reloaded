<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:aop="http://www.springframework.org/schema/aop" xmlns:context="http://www.springframework.org/schema/context" xmlns:jee="http://www.springframework.org/schema/jee" xmlns:tx="http://www.springframework.org/schema/tx" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd   http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd   http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd   http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee.xsd   http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

	<!-- make sure that the properties bean is defined and beans defined in 
		this file can be configured properly. -->
	<import resource="classpath*:/META-INF/spring/applicationContext-test-ccfruntimeproperties.xml"/>

    <!--
		This will automatically locate any and all property files you have
		within your classpath, provided they fall under the META-INF/spring
		directory. The located property files are parsed and their values can
		then be used within application context files in the form of
		${propertyKey}.
	-->
    <context:property-placeholder location="classpath*:META-INF/spring/*-test.properties"/>
    <!--
		Turn on AspectJ @Configurable support. As a result, any time you
		instantiate an object, Spring will attempt to perform dependency
		injection on that object. This occurs for instantiation via the "new"
		keyword, as well as via reflection. This is possible because AspectJ
		is used to "weave" Roo-based applications at compile time. In effect
		this feature allows dependency injection of any object at all in your
		system, which is a very useful feature (without @Configurable you'd
		only be able to dependency inject objects acquired from Spring or
		subsequently presented to a specific Spring dependency injection
		method). Roo applications use this useful feature in a number of
		areas, such as @PersistenceContext injection into entities.
	-->
    <context:spring-configured/>
    <!--
		This declaration will cause Spring to locate every @Component,
		@Repository and @Service in your application. In practical terms this
		allows you to write a POJO and then simply annotate the new POJO as an
		@Service and Spring will automatically detect, instantiate and
		dependency inject your service at startup time. Importantly, you can
		then also have your new service injected into any other class that
		requires it simply by declaring a field for your service inside the
		relying class and Spring will inject it. Note that two exclude filters
		are declared. The first ensures that Spring doesn't spend time
		introspecting Roo-specific ITD aspects. The second ensures Roo doesn't
		instantiate your @Controller classes, as these should be instantiated
		by a web tier application context. Refer to web.xml for more details
		about the web tier application context setup services.
		
		Furthermore, this turns on @Autowired, @PostConstruct etc support. These 
		annotations allow you to use common Spring and Java Enterprise Edition 
		annotations in your classes without needing to do any special configuration. 
		The most commonly used annotation is @Autowired, which instructs Spring to
		dependency inject an object into your class.
	-->
    <context:component-scan base-package="com.collabnet.ccf.ccfmaster">
        <context:exclude-filter expression=".*_Roo_.*" type="regex"/>
        <context:exclude-filter expression="org.springframework.stereotype.Controller" type="annotation"/>
    </context:component-scan>
	
    <bean class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close" id="dataSource">
        <property name="driverClassName" value="${database.driverClassName}"/>
        <property name="url" value="${database.url}"/>
        <property name="username" value="${database.username}"/>
        <property name="password" value="${database.password}"/>
    </bean>
    <bean class="org.springframework.orm.jpa.JpaTransactionManager" id="transactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactory"/>
    </bean>
    <tx:annotation-driven mode="aspectj" transaction-manager="transactionManager"/>
    <!--
		Note: there is deliberately no src/test/resources/META-INF/persistence.xml any more.
		It was a copy of the main one, differing only in a comment, and both landed on the
		test classpath declaring a unit named "persistenceUnit". Spring 3.0's
		DefaultPersistenceUnitManager silently let the last match win - target/classes, where
		the entity classes are - so the test copy never took effect. From Spring 3.1 on the
		duplicate is a hard error, "Conflicting persistence unit definitions for name
		'persistenceUnit'", and pinning persistenceXmlLocation to the *first* match instead
		would pick target/test-classes and leave Hibernate with no entities at all
		("Could not resolve root entity 'Direction'"). Removing the copy is what keeps the
		persistence unit root where it has always been.
	-->
    <bean class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean" id="entityManagerFactory">
        <property name="dataSource" ref="dataSource"/>
    </bean>
    
   	<bean class="com.collabnet.ccf.ccfmaster.server.core.MockLandscapeConfigPersisterFactory" />
   	<bean class="com.collabnet.ccf.ccfmaster.server.core.MockParticipantConfigPersisterFactory" />
   	<bean class="com.collabnet.ccf.ccfmaster.server.core.MockDirectionConfigPersisterFactory" />
   	
   	<bean class="com.collabnet.ccf.ccfmaster.server.fieldmapping.xsl.MockFieldMappingLandscapeTemplatePersisterFactory" />
   	<bean class="com.collabnet.ccf.ccfmaster.server.fieldmapping.xsl.MockFieldMappingExternalAppTemplatePersisterFactory" />
   	<bean class="com.collabnet.ccf.ccfmaster.server.fieldmapping.xsl.MockFieldMappingPersisterFactory" />
   	
   	<bean class="com.collabnet.ccf.ccfmaster.server.core.MockExternalAppCreationListenerFactory" />
   	<bean class="com.collabnet.ccf.ccfmaster.server.core.MockLandscapeCreationListenerFactory"/>
   	<bean class="com.collabnet.ccf.ccfmaster.server.core.MockLandscapeModificationListenerFactory" />
   	
   	<bean class="com.collabnet.ccf.ccfmaster.server.core.MockLandscapeCCFCoreInteractionStrategy" />
   	
   	<bean class="com.collabnet.ccf.ccfmaster.server.core.MockDirectionCCFCoreInteractionStrategy" />
   	
   	<bean class="com.collabnet.ccf.ccfmaster.server.domain.LogFileFactory">
   		<property name="baseDirectory" value="/tmp"/>
   	</bean>
    
    <bean class="com.collabnet.ccf.ccfmaster.server.core.MockRepositoryIdBelongsToExternalAppListenerFactory" />
    
   	<bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<bean id="ccfMasterBackup" class="com.collabnet.ccf.core.utils.CCFMasterBackUp">
		<property name="archiveLocation" value="#{CCFRunTimeProperties.ccfHome}/archive" />
		<property name="ccfHome" value="#{CCFRunTimeProperties.ccfHome}" />
		<property name="dbPath" value="#{CCFRunTimeProperties.ccfHome}/CCFDB" />
		<property name="jdbcTemplate" ref="jdbcTemplate" />
		<property name="startCoresOnBootBean" ref="startCoresOnBootBean" />
	</bean>
	
	<bean id ="startCoresOnBootBean" class="com.collabnet.ccf.ccfmaster.server.core.StartCoresOnBootBean"
		 init-method="boot"	destroy-method="shutdown" />
</beans>
